version: "3.5"
services:
  centos7php80dev:
    build:
      context: dev
      args:
        centos: $OS7
    image: aursu/php80build:${OS7TAG}-dev
  centos8php80dev:
    build:
      context: dev
      args:
        centos: $OS8
    image: aursu/php80build:${OS8TAG}-dev
