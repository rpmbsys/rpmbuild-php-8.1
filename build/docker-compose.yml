services:
  rocky9build:
    build:
      context: .
      args:
        rocky: $RL9
    image: "aursu/php81build:${RL9TAG}-build"
  rocky10build:
    build:
      context: .
      args:
        rocky: $RL10
    image: "aursu/php81build:${RL10TAG}-build"